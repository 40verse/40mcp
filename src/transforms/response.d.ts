/**
 * 40mcp/transforms — TypeScript declarations for the `./transforms` subpath export.
 *
 * Public entry point: `import { ... } from '40mcp/transforms'`.
 * Canonical declarations live in `../index.d.ts`; re-exported here to keep
 * a single source of truth.
 */

export {
  ResponseTransform,
  applyResponseTransform,
} from '../index.js';
