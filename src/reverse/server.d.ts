/**
 * 40mcp/reverse — TypeScript declarations for the `./reverse` subpath export.
 *
 * Public entry point: `import { ... } from '40mcp/reverse'`.
 * Canonical declarations live in `../index.d.ts`; re-exported here to keep
 * a single source of truth.
 */

export {
  ReverseBridgeConfig,
  ReverseBridgeInstance,
  createReverseBridge,
  generateOpenApiSpec,
} from '../index.js';
